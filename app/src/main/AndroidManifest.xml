<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reetadhinglani.jay_julelal">

    <application
        android:allowBackup="true"
        android:icon="@drawable/julelal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/julelal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Story_List" />
        <activity android:name=".StoryDetail" />
        <activity android:name=".hindhiStory_List" />
        <activity android:name=".hindhiStoryDetail" />
        <activity android:name=".select_lang"></activity>
    </application>

</manifest>